var React = require('react');

var About = (props) => {
    return (
        <div>
            <h1 className="text-center page-title">About</h1>
            <p>React learning app</p>
        </div>
    );
};

module.exports = About;